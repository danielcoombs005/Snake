import React from 'react';
import SnakeMain from './SnakeApp/SnakeMain';
import SnakeGrid from './SnakeApp/SnakeGrid';

function App() {
  return (
    <div>
      Main App
      {<SnakeMain />}
    </div>
  );
}

export default App;
